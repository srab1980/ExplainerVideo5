# Environment Configuration
# Copy this file to .env.local for local development
# Or to .env for production

# App Configuration
NEXT_PUBLIC_APP_NAME="Enhanced Next.js App"
NEXT_PUBLIC_ENVIRONMENT="development"

# API Configuration
NEXT_PUBLIC_API_URL="http://localhost:3000"

# Database Configuration
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
# Example (local): postgresql://postgres:password@localhost:5432/next_app_db?schema=public
# Example (cloud): Get from your cloud provider (Railway, Supabase, Neon, etc.)
DATABASE_URL="postgresql://postgres:password@localhost:5432/next_app_db?schema=public"

# Authentication
# JWT secret for token signing (generate a secure random string)
# You can generate one with: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# Feature Flags
NEXT_PUBLIC_ENABLE_ANALYTICS="false"
NEXT_PUBLIC_ENABLE_ERROR_REPORTING="true"
NEXT_PUBLIC_ENABLE_PERFORMANCE_MONITORING="true"

# Security (server-side only)
# API_SECRET_KEY="your-secret-key-here"

# Third-party Services (add as needed)
# SENTRY_DSN="your-sentry-dsn-here"
# ANALYTICS_ID="your-analytics-id-here"

# Email Configuration (for future use)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASSWORD="your-app-password"
# EMAIL_FROM="noreply@yourapp.com"